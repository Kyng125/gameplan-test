import { Inter, Audiowide, Mate_SC } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const audiowide = Audiowide({
    weight: ['400'],
    subsets: ['latin'],
});

export const mate_sc = Mate_SC({
    weight: ['400'],
    subsets: ['latin'],
})